<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="ifrs.canoas.ifhelper.MainActivity"
    tools:showIn="@layout/activity_main">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:ignore="UselessParent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/bem_vindo_ao_ifhelper"
            android:id="@+id/textView"
            android:textColor="@color/material_deep_teal_500" />

        <Button
            android:id="@+id/btCalculaFoto"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="51dp"
            android:drawableLeft="@android:drawable/ic_menu_month"
            android:onClick="calculaFaltas"
            android:text="@string/btn_calculadora_faltas"
            tools:ignore="RtlHardcoded" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_portal_aluno"
            android:id="@+id/btPortalDoAluno"
            android:onClick="calendarioAcademico"
            android:drawableLeft="@drawable/ic_school_red_24dp"
            android:drawableStart="@drawable/ic_school_red_24dp" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_calendario_academico"
            android:drawableLeft="@android:drawable/ic_menu_today"
            android:id="@+id/button"
            android:drawableStart="@android:drawable/ic_menu_today" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_calcula_notas"
            android:drawableLeft="@android:drawable/ic_menu_add"
            android:onClick="calculaNota"
            android:drawableStart="@android:drawable/ic_menu_add" />
    </LinearLayout>

</RelativeLayout>
